<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-12 mb-12">
  <h2 class="text-left text-pretty text-4xl font-semibold tracking-tight text-gray-900 sm:text-5xl">Alpha Bloggers (w/ a hard -er)</h2>
  <p class="mt-4 text-lg/8 text-gray-600 mb-6">Listen to my crazed rants and ramblings as if you asked an old prespector "where the gold in them there hills are."</p>
  
  <div class="h-px bg-gray-200 mt-4 mb-0"></div>

</div>

<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mb-8 sticky top-0 bg-white z-10 py-4">
  <%= will_paginate @users, 
      class: "flex justify-center space-x-1",
      previous_label: "← Prev",
      next_label: "Next →" %>
</div>

<% @users.each do |user| %>
  <div class="bg-white py-8 sm:py-12">
    <div class="mx-auto max-w-8xl px-8 lg:px-8">
      <div class="mx-auto max-w-2xl lg:max-w-4xl">
        <div class="mt-4 space-y-20 lg:mt-16 lg:space-y-20">
          <!-- Left side - Image -->
          <article class="relative isolate flex flex-col gap-8 lg:flex-row">
            <div class="relative aspect-video sm:aspect-[2/1] lg:aspect-square lg:w-64 lg:shrink-0">
              <img src="https://images.unsplash.com/photo-1496128858413-b36217c2ce36?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=3603&q=80" alt="" class="absolute inset-0 size-full rounded-2xl bg-gray-50 object-cover">
              <div class="absolute inset-0 rounded-2xl ring-1 ring-inset ring-gray-900/10"></div>
            </div>
            <div>
              <div class="flex items-center gap-x-4 text-xs">
                <%= link_to user.username, user %>
              </div>
              <div class="group relative max-w-xl">
                <h3 class="mt-3 text-lg/6 font-semibold text-blue-800 group-hover:text-blue-400">
                  <%= link_to user_path(user) do %>
                    <span class="absolute inset-0"></span>
                    <%= pluralize(user.articles.count, "article") %>
                  <% end %>
                </h3>
                
              </div>
              <div class="mt-6 flex border-t border-gray-900/5 pt-6">
                <div class="relative flex items-center gap-x-4">
                  <img src="https://images.unsplash.com/photo-1519244703995-f4e0f30006d5?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=facearea&facepad=2&w=256&h=256&q=80" alt="" class="size-10 rounded-full bg-gray-50">
                  <div class="text-sm/6">
                    <p class="font-semibold italic text-gray-900">
                      <a href="#">
                        <span class="absolute inset-0"></span>
                        by <%= user.username %>
                      </a>
                    </p>
                    <p class="text-gray-600">Co-Founder / CTO</p>
                  </div>
                </div>
              </div>
            </div>
            <div class="flex flex-col items-left space-y-10 lg:w-32">
              <% if logged_in? %>
                <% if user == current_user %>
                  <%= link_to "Edit", edit_user_path(user), class: "w-full bg-gray-500 hover:bg-gray-700 text-white font-semibold py-2 px-4 rounded shadow text-center no-underline mb-4" %>
                <% end %>
                <% if current_user.admin? %>
                  <%= link_to "Delete user", user_path(user), class: "w-full bg-red-600 hover:bg-red-700 text-white font-semibold py-2 px-4 rounded shadow text-center no-underline mb-4", data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this account and all articles?"} %>
                <% end %>
              <% end %>
            </div>
          </article>

          <!-- More posts... -->
        </div>
      </div>
    </div>
  </div>
<% end %>

<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mb-8 sticky top-0 bg-white z-10 py-4">
  <%= will_paginate @users, 
      class: "flex justify-center space-x-1",
      previous_label: "← Prev",
      next_label: "Next →" %>
</div>